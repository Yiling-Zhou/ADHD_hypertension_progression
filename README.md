# ADHD and cardiorenal trajectory after hypertension: demo package

## Contents
- `Step2-conditional transition rate.qmd`
- `Microsimulation logical structure codes.qmd`
- `Simulated Demo data generating.R`

## Software requirements
- R >= 4.2.3
- Packages: data.table, survival, mstate, tableone, ggplot2, IRanges, survminer, tibble, zoo, forecast, foreach, doParallel

## Tested on
- Windows 11
- R 4.2.3

## `Simulated Demo data generating.R`
This is a R code generated by GPT 5.4 for randomly simulating demo data.

## expected output
results/P1_participants_excluded.csv
results/P2_S1_descriptives_result.csv
processData/adhd/dt_covariate.csv
processData/adhd/dt_ms.csv
paste0("processData/",part,"/model_transition", transition_number, ".rds")
processData/adhd/fit_poisson.rds

## Installation
Installation R based on R offical website.
Install required packages in R:
```r
install.packages(c(
  "data.table", "survival", "mstate", "tableone", "ggplot2",
  "survminer", "tibble", "zoo", "forecast", "foreach", "doParallel"
))
if (!requireNamespace("BiocManager", quietly = TRUE)) install.packages("BiocManager")
BiocManager::install("IRanges")

